{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Descrp</th>
                <td>{{ commentaire.descrp }}</td>
            </tr>
            <tr>
                <th>Temps_comm</th>
                <td>{{ commentaire.tempsComm ? commentaire.tempsComm|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentairess_index') }}">back to list</a>

    <a href="{{ path('app_commentairess_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentairess/_delete_form.html.twig') }}
{% endblock %}
